# YBBF (용인시보디빌딩협회) 플랫폼 디자인 및 개발 가이드라인

## 1. 프로젝트 개요
- **목적**: 용인시보디빌딩협회(YBBF) 공식 홈페이지
- **테마**: 시네마틱 다크 무드, 거대한 타이포그래피, 가로/세로 복합 스크롤 스토리텔링
- **주요 대상**: 보디빌딩 카테고리 (F1을 보디빌딩 씬으로 치환)
- **언어**: 한국어

## 2. 기술 스택
- **프레임워크**: React (Vite)
- **스타일링**: Tailwind CSS + CSS Variables
- **애니메이션**: GSAP (ScrollTrigger 포함), Lenis (스무스 스크롤), SplitType (텍스트 쪼개기)
- **아이콘**: Lucide React (SVG 아이콘 필수, 이모지 금지)

## 3. 디자인 시스템

### 3.1. 컬러 팔레트 (CSS 변수)
```css
:root {
  --bg-primary: #0a0a0a;       /* 가장 어두운 배경 */
  --bg-secondary: #161a16;     /* 서브 배경 */
  --bg-card: #1d1f1d;          /* 카드 배경 */

  --text-primary: #f5f5f0;     /* 주 텍스트 (오프 화이트) */
  --text-muted: #888888;       /* 보조 텍스트 */
  --text-inverted: #0a0a0a;    /* 반전 텍스트 */

  --accent: #c4ff00;           /* 시그니처 형광 라임 (강조용) */
  --accent-dark: #2d4a1f;      /* 다크 올리브 */

  --divider: rgba(255, 255, 255, 0.08); /* 구분선 */
}
```

### 3.2. 타이포그래피 (Google Fonts)
- **Display**: Anton (대문자, 타이틀용 고딕)
- **Body**: Pretendard 또는 Inter (본문용)
- **디자인 원칙**: 아주 크고 두꺼운 타이틀과 디테일한 캡션 텍스트의 대비를 이용.

### 3.3. 애니메이션 원칙 (GSAP + ScrollTrigger)
1. **스무스 스크롤 (Lenis)**: 브라우저 기본 스크롤을 부드럽게.
2. **등장 애니메이션 (Scroll Reveal)**: 텍스트는 SplitType을 사용해 단어/글자 단위로 밑에서 위로 떠오르도록 (Stagger).
3. **가로 스크롤 (Horizontal Scroll)**: 특정 섹션(갤러리 등)에서는 세로 스크롤을 가로 이동으로 변환.
4. **패럴랙스 (Parallax)**: 백그라운드 이미지들이 스크롤/마우스 이동에 맞춰 미세하게 움직이도록 구현.

## 4. 페이지 구성 요소
1. **로더 (Loader)**: 페이지 진입 시 로고 타이포그래피 로딩 후 사라짐.
2. **글로벌 네비게이션**: 햄버거 메뉴, 투명 배경 및 스크롤 시 Blur.
3. **히어로**: 압도적 크기의 `YBBF` 타이틀과 풀스크린 배경.
4. **매니페스토 (선언문)**: 단어 단위로 나타나는 텍스트.
5. **선수 갤러리 (가로 스크롤)**: 대회 사진 및 명언 등.
6. **무대 온/오프 (On Stage / Off Stage)**: 선수들의 대회 및 일상 듀얼 레이아웃.
7. **명예의 전당 (Hall of Fame)**: 우승 선수/트로피 슬라이드 또는 그리드.
8. **스토어 / 파트너 / 소셜**: 관련 정보 제공 섹션.
9. **푸터**: 거대한 슬로건 및 하단 정보.

## 5. 다음 작업자를 위한 주의사항
- **SVG 전용**: 사이트 내 모든 시각적 요소(화살표, 사인 등)는 이모지가 아닌 SVG를 사용할 것.
- **GSAP 클린업**: React 환경이므로 `gsap.context()`나 `@gsap/react`의 `useGSAP`을 적극 활용하여 컴포넌트 언마운트 시 ScrollTrigger 메모리 누수를 방지. (이 프로젝트에서는 기본 `useEffect` 내에서 cleanup 반환)
